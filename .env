# ─── Database ────────────────────────────────────────────────────────────────
DB_NAME=bible_compare
DB_USER=bible
DB_PASSWORD=changeme

# ─── App ─────────────────────────────────────────────────────────────────────
# Generate a real secret for production:
#   python -c "import secrets; print(secrets.token_hex(32))"
APP_SECRET=changeme

# FrankenPHP / Caddy server name
# In production replace with your real domain, e.g.: example.com
SERVER_NAME=localhost, localhost:80
