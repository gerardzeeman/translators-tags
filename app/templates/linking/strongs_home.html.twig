{% extends 'base.html.twig' %}
{% block title %}Strong's koppelen – Bijbel Vergelijking{% endblock %}

{% block body %}
<div class="page-strongs-home">
    <h1>Koppelen per Strong's</h1>
    <p>
        Voer een Strong's nummer in om alle voorkomens te bekijken en te koppelen.
    </p>

    <div class="strongs-home-card">
        <div class="form-group">
            <label>
                Vertaling
            </label>
            <select id="strongs-translation">
                {% for t in translations %}
                {% if t.code == 'SV' or is_granted('ROLE_VIEWER_HSV') %}
                <option value="{{ t.code }}">{{ t.name }}</option>
                {% endif %}
                {% endfor %}
            </select>
        </div>
        <div class="form-group">
            <label>
                Strong's nummer
            </label>
            <input type="text"
                   id="strongs-search"
                   placeholder="bijv. H0430 of G2316">
            <p class="field-hint">
                H = Hebreeuws (OT) &nbsp;·&nbsp; G = Grieks (NT)
            </p>
        </div>
        <button id="strongs-btn" class="btn-primary">
            Zoek woord →
        </button>
    </div>
</div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script src="{{ asset('strongs_search.js') }}" defer></script>
{% endblock %}
