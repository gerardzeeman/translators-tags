services:
    _defaults:
        autowire: true
        autoconfigure: true

    App\:
        resource: '../src/'
        exclude:
            - '../src/DependencyInjection/'
            - '../src/Entity/'
            - '../src/Kernel.php'

    App\Repository\PassageRepository:
        arguments:
            $connection: '@doctrine.dbal.default_connection'

    App\Repository\LinkingRepository:
        arguments:
            $connection: '@doctrine.dbal.default_connection'
